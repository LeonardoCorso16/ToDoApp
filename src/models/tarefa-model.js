class tarefas {

    constructor(id,titulo,descricao,status,data,idusuario) {
  
            this.id = id,
            this.titulo = titulo,
            this.descricao = descricao,
            this.status = status,
            this.data = data,
            this.idusuario = idusuario
    }
  
  }
  
  module.exports = tarefas;